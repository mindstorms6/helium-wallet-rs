[package]
name = "helium-wallet-cli"
version = "1.2.3"
authors = ["Helium <info@helium.com>"]
edition = "2018"

description = "A Rust Helium Wallet library"
readme = "README.md"
keywords = ["helium", "blockchain", "wallet"]

homepage = "https://developer.helium.com/blockchain/blockchain-cli"
repository = "https://github.com/helium/helium-wallet-rs"

license = "Apache-2.0"

[[bin]]
name = "helium-wallet"
path = "main.rs"
doc = false

[dependencies]
byteorder = "1.3.2"
structopt = "0.3"
dialoguer = "0.6"
console = "0.11"
hex = "0.4.2"
hmac = "0.7.1"
sha2 = "0.8.0"
base64 = "0"
prettytable-rs = "0.6"
regex = "1"
qr2term = "0.1"
serde_json = "1"
helium-api = { git = "https://github.com/helium/helium-api-rs", tag="1.0.0"}
helium-proto = { git = "https://github.com/helium/proto" }
helium-wallet = { path = "../lib" }

# Add openssl-sys as a direct dependency so it can be cross compiled to
# x86_64-unknown-linux-musl using the "vendored" feature below
openssl-sys = "*"

[features]
# Force openssl-sys to staticly link in the openssl library. Necessary when
# cross compiling to x86_64-unknown-linux-musl.
vendored = ["openssl-sys/vendored"]